The "Token" (which allows access to the discord API) has to be placed in the same folder inside a file called "token.txt", or the code adjusted to do it otherwise. Please don't post tokens to github.
